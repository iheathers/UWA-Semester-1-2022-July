# Write a boolean function is_odd(number) that takes an integer parameter number and returns True if and only if number is odd.
# (Hint: consider the mod operator %).




def is_odd(number):
    if (number % 2 != 0):
        return True
    else:
        return False
    
